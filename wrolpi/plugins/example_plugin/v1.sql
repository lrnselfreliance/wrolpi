-- noinspection SqlNoDataSourceInspectionForFile

-- Create the tables for plugin here.  Don't step on any other plugin's tables!  This will be applied during the DB
-- build/initialization.

CREATE TABLE example_plugin
(
    id SERIAL PRIMARY KEY
);
