"""
WROLPi e-Paper HAT UI (Waveshare 2.7").

Runs as a separate process: ``python -m controller.epaper``.
Talks to the Controller over localhost HTTP; never duplicates host control logic.
"""

__version__ = "0.1.0"
