# If you wish to change these settings, override them in docker-compose.override.yml.  If that override files does not
# exist, create it.  See: https://docs.docker.com/compose/extends/

version: '3'
services:
  db:
    image: postgres:12
    environment:
      - POSTGRES_DB=wrolpi
      - POSTGRES_PASSWORD=wrolpi
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]

  api:
    depends_on:
      - db
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    ports:
      - ${REACT_APP_API-0.0.0.0:8081}:8081
    volumes:
      - .:/opt/wrolpi_media
      - './config:/opt/wrolpi/config'
    healthcheck:
      test: [ "CMD-SHELL", "curl http://127.0.0.1:8081/api/echo" ]
    user: "${UID-1000}:${GID-1000}"

  archive:
    image: lrnselfreliance/archive
    ports:
      - '8083:8080'
    healthcheck:
      test: [ 'CMD-SHELL', 'curl http://0.0.0.0:8080/' ]

  app:
    depends_on:
      - api
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    ports:
      - '8082:8082'
    healthcheck:
      test: [ "CMD-SHELL", "curl http://127.0.0.1:5000" ]
    user: "${UID-1000}:${GID-1000}"

  web:
    depends_on:
      - app
    image: nginx:latest
    ports:
      - ${WEB_HOST-0.0.0.0}:${WEB_PORT-8080}:80
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf"
      - "./icon.ico:/etc/nginx/favicon.ico"
      - "./icon.png:/etc/nginx/icon.png"
      - "./test:/opt/media"
    healthcheck:
      test: [ "CMD-SHELL", "curl http://127.0.0.1:80" ]
